# Flowise Admin UI credentials
FLOWISE_USERNAME=admin
FLOWISE_PASSWORD=changeme_strong_password_here

# Anthropic API Key — set in Flowise UI credentials, NOT here
# ANTHROPIC_API_KEY is entered via Flowise Credentials panel
# This avoids leaking it in docker-compose logs
